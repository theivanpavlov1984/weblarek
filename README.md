# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

---

## Данные

В приложении используются две основные сущности: **товар** и **покупатель**.

### Интерфейсы

#### `IProduct` — товар
Описывает один товар в каталоге магазина.

| Поле | Тип | Описание |
|---|---|---|
| `id` | `string` | Уникальный идентификатор товара |
| `description` | `string` | Описание товара |
| `image` | `string` | Путь к изображению товара |
| `title` | `string` | Название товара |
| `category` | `string` | Категория товара |
| `price` | `number \| null` | Цена товара; `null` — товар недоступен для покупки |

#### `IBuyer` — покупатель
Описывает данные, которые покупатель вводит при оформлении заказа.

| Поле | Тип | Описание |
|---|---|---|
| `payment` | `TPayment \| null` | Вид оплаты: `'card'` или `'cash'`; `null` — не выбран |
| `email` | `string` | Email покупателя |
| `phone` | `string` | Телефон покупателя |
| `address` | `string` | Адрес доставки |

#### `IOrderRequest` — данные заказа для отправки на сервер
Расширяет `IBuyer`, дополнительно содержит:

| Поле | Тип | Описание |
|---|---|---|
| `items` | `string[]` | Массив id товаров в заказе |
| `total` | `number` | Итоговая сумма заказа |

#### `IProductsResponse` — ответ сервера на GET /product/

| Поле | Тип | Описание |
|---|---|---|
| `total` | `number` | Общее количество товаров |
| `items` | `IProduct[]` | Массив товаров |

#### `IOrderResponse` — ответ сервера на POST /order/

| Поле | Тип | Описание |
|---|---|---|
| `id` | `string` | Идентификатор созданного заказа |
| `total` | `number` | Подтверждённая сумма заказа |

### Типы

`TPayment = 'card' | 'cash'` — допустимые виды оплаты.  
`TBuyerValidationErrors = Partial<Record<keyof IBuyer, string>>` — объект ошибок валидации: ключ — поле покупателя, значение — текст ошибки.

### Интерфейсы моделей данных

Интерфейсы описывают публичный API каждой модели. Они позволяют другим классам (в следующем спринте — Presenter) зависеть от абстракции, а не от конкретной реализации.

`IProductsModel` — интерфейс модели каталога товаров.  
`IBasketModel` — интерфейс модели корзины.  
`IBuyerModel` — интерфейс модели покупателя.

---

## Модели данных

Модели данных — классы слоя **Model**, отвечающие за хранение и управление данными. Они изолированы от отображения и не зависят от других классов приложения.

### Класс `Products`

Хранит каталог товаров и товар, выбранный для детального просмотра.

**Конструктор:** параметры не принимает.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_items` | `IProduct[]` | Массив всех товаров каталога |
| `_preview` | `IProduct \| null` | Товар, выбранный для подробного просмотра |

**Методы класса:**

| Метод | Параметры | Возвращает | Описание |
|---|---|---|---|
| `setItems` | `items: IProduct[]` | `void` | Сохраняет массив товаров |
| `getItems` | — | `IProduct[]` | Возвращает все товары каталога |
| `getItemById` | `id: string` | `IProduct \| undefined` | Возвращает товар по его id |
| `setPreview` | `item: IProduct` | `void` | Сохраняет товар для детального просмотра |
| `getPreview` | — | `IProduct \| null` | Возвращает товар для детального просмотра |

---

### Класс `Basket`

Хранит список товаров, выбранных покупателем для покупки.

**Конструктор:** параметры не принимает.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_items` | `IProduct[]` | Массив товаров в корзине |

**Методы класса:**

| Метод | Параметры | Возвращает | Описание |
|---|---|---|---|
| `getItems` | — | `IProduct[]` | Возвращает список товаров в корзине |
| `addItem` | `item: IProduct` | `void` | Добавляет товар в корзину |
| `removeItem` | `item: IProduct` | `void` | Удаляет товар из корзины |
| `clear` | — | `void` | Очищает корзину |
| `getTotalPrice` | — | `number` | Возвращает суммарную стоимость товаров |
| `getCount` | — | `number` | Возвращает количество товаров в корзине |
| `hasItem` | `id: string` | `boolean` | Проверяет наличие товара в корзине по его id |

---

### Класс `Buyer`

Хранит данные покупателя, которые тот вводит при оформлении заказа.

**Конструктор:** параметры не принимает.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_payment` | `TPayment \| null` | Вид оплаты; `null` — не выбран |
| `_address` | `string` | Адрес доставки |
| `_phone` | `string` | Телефон покупателя |
| `_email` | `string` | Email покупателя |

**Методы класса:**

| Метод | Параметры | Возвращает | Описание |
|---|---|---|---|
| `setField` | `field: keyof IBuyer, value: string` | `void` | Сохраняет одно поле данных покупателя, не затрагивая остальные |
| `getData` | — | `IBuyer` | Возвращает все данные покупателя |
| `clear` | — | `void` | Очищает все данные покупателя |
| `validate` | — | `TBuyerValidationErrors` | Проверяет заполненность полей; возвращает объект с текстами ошибок для незаполненных полей |

---

## Слой коммуникации

Класс коммуникационного слоя отвечает за взаимодействие с API сервера. Использует **композицию**: принимает объект, реализующий интерфейс `IApi`, и делегирует ему выполнение HTTP-запросов.

### Класс `LarekApi`

**Конструктор:**  
`constructor(api: IApi)` — принимает объект, реализующий интерфейс `IApi` (экземпляр базового класса `Api`).

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_api` | `IApi` | Объект для выполнения HTTP-запросов |

**Методы класса:**

| Метод | Параметры | Возвращает | Описание |
|---|---|---|---|
| `getProducts` | — | `Promise<IProductsResponse>` | GET-запрос на `/product/`; возвращает объект с массивом товаров и их количеством |
| `createOrder` | `order: IOrderRequest` | `Promise<IOrderResponse>` | POST-запрос на `/order/`; отправляет данные заказа, возвращает подтверждение с id и суммой |

---

## Слой представления (View)

View-компоненты отвечают только за отображение и за оповещение о действиях пользователя. Они **не хранят данные** — ни в полях, ни в разметке (даже `id`). Внутри представления допускается хранить только: ссылки на элементы своего контейнера, брокер событий (`IEvents`) и колбэки.

Представления изменяются **только при изменении моделей**. Поэтому почти все они создаются однократно и при необходимости перерисовываются методом `render()`. Заново на каждый рендер создаются лишь карточки каталога и корзины — потому что их количество зависит от данных.

Логика клика по карточкам каталога и корзины реализована через **колбэки**: презентер при создании карточки передаёт колбэк, внутри которого уже лежит `emit` с нужными данными. Карточка просто сохраняет колбэк и вызывает его по клику — сами данные внутри карточки не хранятся.

Все компоненты наследуются от базового класса `Component<T>`.

---

### Базовый класс `Component<T>`

Абстрактный базовый класс для всех компонентов представления.

**Конструктор:**  
`constructor(container: HTMLElement)` — принимает корневой DOM-элемент компонента.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `container` | `HTMLElement` | Корневой DOM-элемент (protected) |

**Методы класса:**

| Метод | Параметры | Возвращает | Описание |
|---|---|---|---|
| `setText` | `element: HTMLElement, value: string \| number` | `void` | Устанавливает textContent элемента |
| `setDisabled` | `element: HTMLElement, state: boolean` | `void` | Включает/выключает атрибут disabled |
| `toggleClass` | `element: HTMLElement, className: string, force?: boolean` | `void` | Переключает CSS-класс |
| `setImage` | `element: HTMLImageElement, src: string, alt?: string` | `void` | Устанавливает src и alt изображения |
| `render` | `data?: Partial<T>` | `HTMLElement` | Применяет данные через сеттеры и возвращает корневой элемент. Вызов без аргументов (`render()`) просто возвращает контейнер в его текущем состоянии |

---

### Класс `Header`

Шапка страницы: иконка корзины со счётчиком. Отдельное представление.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает элемент `.header` и брокер событий.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_counter` | `HTMLElement` | Счётчик товаров на иконке корзины |
| `_basketButton` | `HTMLButtonElement` | Кнопка открытия корзины |

**Сеттеры:** `counter: number` — обновляет число на иконке корзины.  
**Генерируемые события:** `basket:open`

---

### Класс `Gallery`

Галерея карточек на главной странице. Отдельное представление.

**Конструктор:**  
`constructor(container: HTMLElement)` — принимает элемент `.gallery`.

**Сеттеры:** `catalog: HTMLElement[]` — заменяет содержимое галереи на переданные карточки.

---

### Класс `Modal`

Управляет модальным окном: открытием, закрытием и содержимым.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает элемент `#modal-container` и брокер событий.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_closeButton` | `HTMLButtonElement` | Кнопка закрытия (крестик) |
| `_content` | `HTMLElement` | Контейнер для содержимого модалки |

**Методы класса:**

| Метод | Описание |
|---|---|
| `open()` | Добавляет класс `modal_active` |
| `close()` | Удаляет класс `modal_active`, очищает содержимое |
| `render(data: IModal)` | Устанавливает содержимое и открывает модалку |

**Сеттеры:** `content: HTMLElement | null` — заменяет содержимое модалки.

---

### Класс `Card<T extends ICard>`

Базовый абстрактный класс для всех карточек товара.

**Конструктор:**  
`constructor(container: HTMLElement)` — принимает корневой элемент карточки.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_title` | `HTMLElement` | Название товара |
| `_price` | `HTMLElement` | Цена товара |

**Сеттеры:** `title`, `price`. Геттеров нет — представление не хранит данные.

---

### Класс `CardCatalog`

Карточка для отображения в каталоге. Наследуется от `Card<ICardCatalog>`. Создаётся заново для каждого товара.

**Конструктор:**  
`constructor(container: HTMLElement, actions?: ICardActions)` — дополнительно находит `_image` и `_category`; если передан колбэк `actions.onClick`, вешает его на клик по контейнеру.

**Дополнительные поля:**

| Поле | Тип | Описание |
|---|---|---|
| `_image` | `HTMLImageElement` | Изображение товара |
| `_category` | `HTMLElement` | Метка категории |

**Дополнительные сеттеры:** `image`, `category`.  
**Логика клика:** через колбэк `onClick` (презентер кладёт внутрь `emit('card:select', item)`).

---

### Класс `CardPreview`

Детальная карточка в модальном окне. Наследуется от `Card<ICardPreview>`. Создаётся однократно, перерисовывается при `preview:changed`.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — дополнительно находит `_image`, `_category`, `_description`, `_button`. Кнопка эмитит одно событие без данных.

**Дополнительные поля:**

| Поле | Тип | Описание |
|---|---|---|
| `_image` | `HTMLImageElement` | Изображение |
| `_category` | `HTMLElement` | Категория |
| `_description` | `HTMLElement` | Описание товара |
| `_button` | `HTMLButtonElement` | Кнопка действия |

**Дополнительные сеттеры:** `image`, `category`, `description`, `buttonText` (текст кнопки), `buttonDisabled` (состояние кнопки). Сеттер `price` наследуется из `Card` и не переопределяется. Какой текст и состояние выставить — решает презентер.  
**Генерируемые события:** `card:buy` (без данных — презентер сам берёт текущий товар из модели превью)

---

### Класс `CardBasket`

Карточка товара в корзине. Наследуется от `Card<ICardBasket>`. Создаётся заново для каждого товара корзины.

**Конструктор:**  
`constructor(container: HTMLElement, actions?: ICardActions)` — дополнительно находит `_index` и `_deleteButton`; если передан колбэк `actions.onClick`, вешает его на кнопку удаления.

**Дополнительные поля:**

| Поле | Тип | Описание |
|---|---|---|
| `_index` | `HTMLElement` | Порядковый номер в корзине |
| `_deleteButton` | `HTMLButtonElement` | Кнопка удаления |

**Дополнительные сеттеры:** `index`.  
**Логика клика:** через колбэк `onClick` (презентер кладёт внутрь `emit('basket:item-remove', item)`).

---

### Класс `BasketView`

Представление корзины: список товаров, итоговая сумма, кнопка оформления.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает клонированный шаблон `#basket`.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_list` | `HTMLElement` | Контейнер списка товаров |
| `_total` | `HTMLElement` | Итоговая сумма |
| `_button` | `HTMLButtonElement` | Кнопка «Оформить» |

**Сеттеры:** `items` (массив карточек; если пусто — показывает «Корзина пуста» и блокирует кнопку), `total`.  
**Генерируемые события:** `basket:checkout`

---

### Базовый класс `Form<T extends IFormState>`

Базовый класс для форм. Управляет состоянием кнопки отправки и сообщением об ошибке.

**Конструктор:**  
`constructor(container: HTMLFormElement, events: IEvents)` — принимает форму и брокер событий. Вешает слушатели на `input` и `submit`.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_submit` | `HTMLButtonElement` | Кнопка отправки формы |
| `_errors` | `HTMLElement` | Блок с сообщением об ошибке |

**Сеттеры:** `valid` (включает/выключает кнопку), `errors` (текст ошибки).  
**Генерируемые события:** `{formName}:change` (при изменении поля, передаёт `{ field, value }`), `{formName}:submit` (при отправке формы)

---

### Класс `OrderPayment`

Форма шага 1: выбор способа оплаты и адрес доставки. Наследуется от `Form<IOrderPaymentView>`.

**Конструктор:**  
`constructor(container: HTMLFormElement, events: IEvents)` — дополнительно находит кнопки выбора оплаты.

**Дополнительные поля:**

| Поле | Тип | Описание |
|---|---|---|
| `_cardButton` | `HTMLButtonElement` | Кнопка «Онлайн» |
| `_cashButton` | `HTMLButtonElement` | Кнопка «При получении» |

**Дополнительные поля:** `_addressInput` — поле ввода адреса.  
**Дополнительные сеттеры:**
- `payment: TPayment | null` — подсвечивает выбранный способ оплаты классом `button_alt-active`;
- `address: string` — записывает значение в поле адреса (нужно, чтобы корректно очищать форму при сбросе модели).

**Генерируемые события:** `payment:select` (передаёт `{ payment }`)

---

### Класс `OrderContacts`

Форма шага 2: ввод email и телефона. Наследуется от `Form<IOrderContactsView>`.

**Конструктор:**  
`constructor(container: HTMLFormElement, events: IEvents)` — дополнительно находит поля `_emailInput` и `_phoneInput`.

**Дополнительные сеттеры:**
- `email: string` — записывает значение в поле email;
- `phone: string` — записывает значение в поле телефона.

У каждой формы есть сеттеры для всех её полей, чтобы при очистке модели (`buyer:changed`) поля ввода тоже очищались.

---

### Класс `Success`

Экран успешного оформления заказа.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` — принимает клонированный шаблон `#success`.

**Поля класса:**

| Поле | Тип | Описание |
|---|---|---|
| `_description` | `HTMLElement` | Текст «Списано N синапсов» |
| `_button` | `HTMLButtonElement` | Кнопка «За новыми покупками!» |

**Сеттеры:** `total` — выводит списанную сумму.  
**Генерируемые события:** `success:close`

---

## События приложения

### События от Моделей данных

| Событие | Источник | Описание |
|---|---|---|
| `catalog:changed` | `Products.setItems()` | Каталог товаров обновлён |
| `preview:changed` | `Products.setPreview()` | Выбран товар для просмотра |
| `basket:changed` | `Basket.addItem/removeItem/clear()` | Содержимое корзины изменилось |
| `buyer:changed` | `Buyer.setField/clear()` | Данные покупателя изменились |

### События от Представлений

| Событие | Источник | Данные | Описание |
|---|---|---|---|
| `card:select` | колбэк `CardCatalog` | `IProduct` | Нажата карточка каталога (товар передаётся в колбэке) |
| `card:buy` | `CardPreview` | — | Нажата кнопка действия в превью; товар презентер берёт из модели |
| `basket:item-remove` | колбэк `CardBasket` | `IProduct` | Нажата кнопка удаления товара (товар передаётся в колбэке) |
| `basket:open` | `Header` | — | Нажата иконка корзины |
| `basket:checkout` | `BasketView` | — | Нажата кнопка «Оформить» |
| `payment:select` | `OrderPayment` | `{ payment }` | Выбран способ оплаты |
| `order:change` | `OrderPayment` | `{ field, value }` | Изменено поле формы шага 1 |
| `order:submit` | `OrderPayment` | — | Нажата кнопка «Далее» |
| `contacts:change` | `OrderContacts` | `{ field, value }` | Изменено поле формы шага 2 |
| `contacts:submit` | `OrderContacts` | — | Нажата кнопка «Оплатить» |
| `success:close` | `Success` | — | Нажата кнопка «За новыми покупками!» |

---

## Презентер

Презентер реализован в основном скрипте `src/main.ts`. Отдельный класс не создаётся — код презентера состоит из обработчиков событий, зарегистрированных через `EventEmitter`.

Презентер:
- создаёт экземпляры всех моделей, View-компонентов и API-класса;
- подписывается на события от моделей и представлений;
- в ответ на события вызывает методы моделей (для изменения данных) или методы представлений (для обновления отображения);
- содержит всю прикладную логику выбора (например, какой текст и состояние выставить кнопке превью — «Купить», «Удалить из корзины» или «Недоступно»);
- не генерирует события самостоятельно.

**Ключевые принципы взаимодействия:**
- представления обновляются **только** в ответ на события моделей (`catalog:changed`, `preview:changed`, `basket:changed`, `buyer:changed`);
- открытие модального окна с корзиной или формами не пересобирает их — берётся актуальное состояние через пустой `render()`, который возвращает корневой элемент;
- карточки каталога и корзины получают данные через колбэки и не хранят их у себя.

**Порядок инициализации:**
1. Создаётся `EventEmitter`.
2. Создаются модели данных (`Products`, `Basket`, `Buyer`) с передачей `EventEmitter`.
3. Создаётся `LarekApi` через композицию с базовым `Api`.
4. Из HTML-шаблонов клонируются и однократно создаются экземпляры представлений (`Header`, `Gallery`, `Modal`, `CardPreview`, `BasketView`, `OrderPayment`, `OrderContacts`, `Success`).
5. Регистрируются все обработчики событий.
6. Выставляется начальное состояние корзины и форм пустыми рендерами.
7. Запрашивается каталог с сервера — `larekApi.getProducts()`.

